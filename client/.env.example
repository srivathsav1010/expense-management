# ─────────────────────────────────────────────────────────────────────────────
# client/.env.example
# Copy to .env.local for local dev, set these as GitHub Secrets for CI/CD
# ─────────────────────────────────────────────────────────────────────────────

# Development (Vite proxy handles /api → localhost:5000, so leave blank locally)
VITE_API_URL=

# Production — your Render backend URL
# VITE_API_URL=https://budgetflow-api.onrender.com/api
